@model IEnumerable<MovieStore.Core.Entities.Movie>

<div>
    <div class="container-fluid">
        <div class="row">
            @foreach (var movie in Model)
            {
                <partial name="_MovieCard" model="movie" />
                @* await Html.PartialAsync("_MovieCard", movie)*@
            }
        </div>
    </div>
</div>
@*
    entity will be shared throught the app.
    repository read from database, data layer: get my movies from the database
    ; servie is business logic layer: get movies and see the ratings;
*@
@*
    build HTML dynamically: 10 movies, foreach loop
*@